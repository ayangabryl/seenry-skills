# Seenry visual candidate

A complete alternative to the existing Seenry marketing example. Open `index.html` using a local server from the repository root. The page reuses the existing Held teaching study and licensed font/image assets through relative links; keep the `examples/seenry-site` sibling available.

Jev selected the product-canvas information structure from text descriptions. The host agent authored the copy, HTML/CSS and behavior. This is not a website generated by Jev or evidence of a quality improvement on unseen briefs. The before example remains unchanged.

`wireframes.html` retains the three planning options. Evaluation inputs, actual API output and review evidence live under `evals/jev-visual-trial`. Fonts, furniture imagery and Held provenance remain documented in `../seenry-site/BUILD.md`. The reference collage is illustrative composition, not a screenshot of the MCP library.
