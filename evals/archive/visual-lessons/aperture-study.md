# Working hypothesis: reserve the editing frame

Original Astra-authored teaching study, separate from the Luna experiments. No human approval or model-transfer improvement is claimed.

Task: crop the supplied NASA/SDO image and export an800px-widePNG. Photo, content, colors and controls remain the same. Compare two deliberate composition choices: a fluid viewport that maximizes the selected image at its ratio, and a fixed viewport whose crop aperture changes while controls stay stationary. This is a narrow comparison, not a three-direction website benchmark.

Use the actual image as the material. Neutral surround, Inter, real Lucide icons. One object title, ratio choices, zoom, reset, dimensions and export. Matte outside the frame supplies context; no duplicate output thumbnail. Native semantic controls and keyboard panning remain available. Crop geometry always describes actual source pixels.

Motion score: select ratio → aperture and image geometry retarget together → settled correct crop. Pointer manipulation stays direct. Export → actual encoding/download handoff → genuine Download/Check morph and truthful status. Rapid changes retain the latest state; reduced motion presents identical content immediately.

Countercase: a fixed stage gives less screen area to a panoramic crop. Fluid layout can be preferable for close inspection or a standalone editor with fewer surrounding controls. Neither is universally premium. The reference lesson concerns control travel and image context, not a new global fixed-height rule.

NASA source: https://sdo.gsfc.nasa.gov/gallery/firstlight/ . SDO multiband composite,30March2010. Instrument-band color is not ordinary visible-light photography. Informational study, no NASA endorsement. Existing source bytes retained.

## Recorded evidence

The source-pixel PNG checks passed at1440/390px viewports. With the component settled before input, changing1:1 to16:9 moved the Export action0px in the fixed treatment, and150.5px/125.125px upward in the fluid treatment. Both had intermediate icon-morph paths after the actual download handoff. These are bounded browser observations, not human preference or physical-device/smoothness certification.

The same photograph and control set are present in both. Compare the size available for inspection, retained image context, and control travel. A successful functional assertion does not select the best relationship for another product.
