# Historical development evidence

These files were removed from installable skill guidance on 2026-09-22. They preserve rejected outputs, unaccepted candidates, authored mechanical comparisons, scoped human feedback, source-specific study narratives and provenance. They are not quality standards, examples to imitate or an accepted-design gallery.

The former installed locations were `skills/seenry/references/lessons`, `skills/seenry/assets/studies` and `skills/seenry/references/studies`. Original captures and feedback remain unchanged. Research notes retain their original relative links as historical text. Reproduce the original runnable environment from Git revision `0f5f4ac` rather than assuming archived HTML paths still form a relocated application.

`lesson_packet.py` is the prior image-selection helper, explicitly rooted at this archive. It supports historical diagnostics only. `render_lessons.mjs` is the prior renderer, also explicitly rooted here; rerender only in a copied experiment directory, not over frozen historical captures. The current skill helper cannot load schema-1 archived image lessons.

The normal installer copies only `skills/`; no archive is required for offline skill use. Third-party font/icon/media provenance remains with the archived files. Current adaptation licenses remain in `skills/seenry/licenses`.
