# Seenry-guided revision

See [BRAND.md](BRAND.md) for the persistent rules. This is an agent-authored refinement using Seenry after the user rejected the earlier showcase, not a fresh independent Sol generation. The original no-skill Sol website is the unchanged before.

Refinement goals: bring evidence into the first viewport, remove decorative labels and outlined lettering, reduce repeated promotional prose, use white/gray UI and consistent charcoal controls, preserve original SVG assets and functional behavior. Same content and assets, changed composition, wording and shared styling. User acceptance remains separate from behavior checks.

Verified in the browser: the before/after switch; inquiry required-field messages and demo-only success; Escape closure; Interiors filter shows Hill Apartment only; narrow iframe (316px client) and mobile host (375px client) without horizontal overflow. A cramped mobile text/action row was repaired by stacking below 420px. The original source is unchanged.

## 24 September: three-version comparison
The visible baseline is now a separate Luna generation, not Sol. This core version remains an authored refinement of the earlier implementation, not a new controlled same-model experiment. Reapplied the Seenry refinement workflow: retained the documented BRAND.md, original illustrations and service facts; checked content grouping and narrow navigation; added keyboard-operable project detail dialogs so the same core tasks exist in all three versions. No MCP research was newly applied to this core version. Core composition is proposition → illustrative study → equal project catalog → inquiry. No claim that a particular model or skill guarantees quality.
