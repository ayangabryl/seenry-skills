# Fieldwork fieldwork-stack

Author-assisted revision by the current agent; not an independent model benchmark. Same fictional content and original SVG artwork as the baseline.

Brand owner: [BRAND.md](BRAND.md), implemented in index.html.

No new MCP reference used for this variant. Core Seenry, Branding, Assets and Motion guidance informed the revision.

Changes: selectable project viewer, neutral brand roles, stable navigation and controls, explicit asset provenance, reduced-motion fallback, shared inquiry states. The MCP version is an adaptation, not a reproduction.

## Checks
Site production build and skill validator passed. Browser: project next/previous selection, MCP inquiry opening/Escape; stack viewed at 390×844 with no horizontal overflow (375px content width). Existing baseline form validation is preserved. Full source animation fidelity is not claimed.
