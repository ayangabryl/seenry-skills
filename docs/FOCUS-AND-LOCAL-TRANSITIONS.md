# Focus and local product transitions

This candidate addresses the sharing refinement feedback: a detached link-permission control, a layered input halo, awkward result copy and missing transition continuity. These are scoped defects, not evidence that all outlines, colors, shadows or native controls are undesirable.

## What changed

The controls guide makes parent/dependent grouping explicit and routes custom focus work to a small original focus guide. The motion guide points to original local recipes for contextual settings, menu origins, copy feedback, item changes and numbers. The working contract carries user copy constraints into the final state checks. It does not make one person's punctuation preference a universal rule.

Focused packets previously returned before resolving selected motion helpers. They now include the chosen source, required runtime files, hashes and license-bearing dependency closure. Missing or altered pinned dependencies fail instead of disappearing from the handoff. Utility recipes are not loaded for scroll-only work. The default focused decision still supplies one working example; no additional MCP call or subscription is required.

## Research boundary

A public motion catalog informed the category audit for menu, card, icon and text transitions. The investigation inspected public descriptions, not playback-derived timing measurements or paid resources. No implementation text or snippets were copied. The proposed timing values are Seenry starting recipes to test in context. Existing original helpers remain the runtime, with the local guidance in [product transitions](../skills/seenry-motion/references/product-transitions.md).

Focus guidance uses [WCAG Focus Visible](https://www.w3.org/WAI/WCAG22/Understanding/focus-visible.html), [Focus Appearance](https://www.w3.org/WAI/WCAG22/Understanding/focus-appearance.html), [MDN focus-visible](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:focus-visible) and the [APG menu-button pattern](https://www.w3.org/WAI/ARIA/apg/patterns/menu-button/). Focus Visible is AA. The stronger area and change-contrast criterion is AAA. A CSS outline recipe alone certifies neither.

## Evidence boundary

`luna-sharing-focus-012` supplies GPT-5.6-Luna high with the existing component, the two user screenshots, the original factual brief, the controls packet and explicitly selected focus/motion resources. It is a feedback-assisted repair. The host supplies guidance and tests but does not edit interface code. Supplied resource hashes, observed execution, author claims, host checks and visual acceptance remain distinct.

Package validation and 182 Python tests pass on macOS, including relocation, focused scope and missing-runtime behavior. Windows, Linux and physical mobile behavior were not executed for this change. Local installation preserves existing agent links and saves a rollback manifest. Component outcomes are recorded in the experiment directory after browser verification. No comparative win or release claim follows from these package checks.

Pixel review exposed a limitation in the initial browser checks: a permission menu inside a clipped disclosure had a nonzero rectangle but no visible menu surface. The focused trigger's outline was also cut off. The additional regression samples hit targets before clicking and compares focus extents with clipping ancestors. It fails the previous implementation at 320 and 1000 pixels. This is a functional correction to the review method, not a visual-quality score. The small role-summary wording fix is recorded separately from design revisions.
