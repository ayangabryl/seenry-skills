# Component direction sprint, 12 September 2026

A 30-minute diagnostic tested a fresh bill splitter and the skills that guide it. The brief used USD 84.50, a 15% tip, three named people, editable bill/tip, one to six people, exact integer-cent shares, clipboard recovery and reset.

## What changed

- Creative wireframe/type reviews can explicitly assess `concept` independently of content, hierarchy and geometry. The request schema and recorder enforce that recorded criterion together; final review retains five criteria.
- Brand guidelines describe consequential component usage through actual source/API, variants, state ownership, keyboard/focus, responsive/motion behavior and contextual exceptions.
- A new palette stays provisional until compared on the same composition. Inherited brand roles remain authoritative. This requirement sits in the compact visual guide already delivered by focused surface packets.

## What the output established

The current-model author made three working wireframes. A separate reviewer received the factual brief and rendered views without the author's rationale, then selected the people-around-total direction. The author had already begun a receipt construction before that decision arrived; it was preserved and the selection was relayed explicitly. This chronology is not three independent finished designs.

The first finish had correct tested bill/share arithmetic, editing and clipboard recovery. The user nevertheless rejected its color and motion quality. The author reported inventing a literal green-table treatment without an earlier palette comparison; it had read the compact visual guide but had not opened the detailed color guide. The subsequent repair compares neutral treatments and addresses a measured button displacement. A local improvement in clarity of the instruction does not yet prove improved future adherence.

This was native agent execution, not an end-to-end recorder run. The requested Gemini Flash call failed authentication before generation. The artifact author used the inherited current Codex model, authorized by the user as Astra; no separate serving-model attestation is available. Product code was authored by that agent, with independent model reviews, host tests and the user's rejection supplied as assistance. It used MCP reference research, so this is not an offline generation experiment.

160 Python tests and package validation pass on macOS. Those checks establish routing, schemas, evidence rules and package integrity. They cannot overrule the visual rejection or establish premium output, competitor superiority, causal skill gains or physical Windows/Linux/mobile coverage.

## Evidence and status

Local run ID: `seenry-thirty-minute-20260912`. Its evidence contains source hashes, first wireframes and finish, browser probes, the exact human rejection, failed Flash transport and separate repair captures. No private authentication logs or copied reference media are included in this package.

The repaired graphite surface preserves the selected arrangement and has a compact alternative documented by its project BRAND.md. The repeated suite passed 184 core and 32 compact visible assertions and three clipboard scenarios. Measured Copy action displacement dropped from 13px to 0px in the same narrow interrupted sequence. A later local currency-label contrast correction passes its targeted recheck (4.961:1); all 15 sampled text roles pass. Prior behavior results remain bound to their actual sources.

The user reviewed the revision and answered “Improved, still not enough.” Its status is needs-revision, not visually accepted. Normal/reduced-motion recordings and sampled trajectories exist, but the independent model's normal-speed perceptual smoothness judgment remains unverified. The first-finish rejection remains in history. Assertion counts are not independent design benchmarks or aesthetic scores. Model token cost for native author/reviewer contexts was not measured.
