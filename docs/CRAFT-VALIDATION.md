# Candidate validation — 20 September 2026

Implemented in branch `codex/decision-craft`; not a published release.

Passed on this macOS host:
- Package validation: five skills, 18 tool schemas, 22 example calls; support links and bundled runtime hashes.
- 164 Python tests, including decision routing, missing-resource failure, relocation, offline support, temporary agent-directory symlink installation and rollback.
- Four Node transition-contract tests.
- Six browser examples: interaction behavior, no page errors and no horizontal overflow at 320px. Motion checks cover retained input, collapsed focus recovery, rapid reversal and reduced interpolation. Color uses independently computed contrast expectations.
- Benchmark harness fixture: preparation, condition resources, anonymous export and separate acceptance/preference gate.

Not established:
- Design quality improvement or comparison wins.
- Windows/Linux runtime browser behavior; portable code and temporary-path tests are not physical cross-platform evidence.
- Full Flash, Luna or Astra comparisons; no generated candidate has received user acceptance.

Flash preflight requested `gemini-3.8-flash-high`. AGY reported missing Antigravity authentication before any model response. Requested identity was recorded; configured/reported identity and usage are unknown. No substitute model was used. Sign-in is required before benchmark execution. Later website/transfer stages and release remain gated.

The five component prompts and candidate resources are frozen separately in development evidence. Keep those runs private until anonymous generated-site export; never serve raw CLI logs. The global installed package remains unchanged by this candidate.
