# Decision-craft candidate release gate

Status: unvalidated candidate. Do not publish while a required gate is missing or failed.

1. Package: resources resolve; narrow routing stays relevant; offline examples function; relocated install and shared symlink/rollback checks pass.
2. Flash component: identical appointment-rescheduling brief, five conditions (no skill, frozen Seenry, revised Seenry, relevant Jakub, relevant Emil). Equal 1200s maximum, tools and facts, MCP/image generation off. Check date selection, unavailable slots, confirmation, cancellation and keyboard recovery. User accepts revised and prefers it to frozen before website stage.
3. Flash website: identical furniture-restoration brief, same five conditions, 2400s maximum. Check asset loading, ordinary/narrow composition, local inquiry and motion. User accepts revised and prefers it to frozen. Across both briefs revised wins at least three of four competitor comparisons. Preference and absolute acceptance are separate.
4. Luna transfer: frozen/revised on upload recovery and community-garden website. Accepted revised outputs, no critical failures, revised preferred for both.
5. Astra: one matched component pair without critical or user-observed quality regression.

Freeze versions, model identities, prompts and tools before each pair. Preserve outputs, failed attempts, usage and time. Transport/model mismatches invalidate tests. No stronger-model corrections to lower-model outputs. Keep condition mapping separate from anonymous preview. Failure blocks later stages; missing review is pending. Results support only tested scopes.

Keep published and installed baselines separately. Development does not replace global skills. Publish/update installations only after gates pass; preserve the prior release and installer backup manifest.
