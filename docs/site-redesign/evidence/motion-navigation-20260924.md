# Motion library organization

Main skill page retains one representative Fluid surfaces playground. The 18-example catalog and duplicate number study no longer extend the landing page. Dedicated /motion routes organize effect families, with /motion/patterns housing the searchable interface examples.

FocusSearch is shared by the live pattern filter and its demonstration. Focus/nonempty input reveals a 36px close control over 220ms; Escape clears and returns focus to the stable wrapper. Blur only closes an empty field when focus actually leaves the group. Reduced motion removes the decorative transition.

Blur / Numbers demonstrates changing digit columns with a fixed three-slot footprint, blank leading slots, one semantic current value and a 99↔100 carry control. Copy prompts are selected by effect and context, including exact settings. Blur/search no longer recommend installing the liquid engine unnecessarily.

Self-review: build and skill validation pass. Browser checked search filtering/reset, focused close disclosure, digit carry, actual clipboard recipe, desktop group links and 390px mobile width. Mobile grid min-content overflow was found and fixed; measured document and viewport width both 390px afterward.
